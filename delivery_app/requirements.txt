# FastAPI framework
fastapi~=0.114

# ASGI server
hypercorn~=0.17

# Pydantic v2 para validación de schemas
pydantic~=2.9

# ORM y base de datos
SQLAlchemy~=2.0
aiosqlite~=0.20

# RabbitMQ async client
pika==1.3.2
aio-pika

# Logs coloridos opcionales
coloredlogs~=15.0

# YAML (opcional)
PyYAML~=6.0

# Dependencias de seguridad (JWT)
python-jose~=3.3
passlib[bcrypt]~=1.7

# HTTP requests asíncronas (por si necesitas comunicarte con otros microservicios)
httpx~=0.24

# Alembic para migraciones (opcional si más adelante quieres versionar la DB)
alembic~=1.12
